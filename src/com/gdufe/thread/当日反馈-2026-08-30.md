# 当日反馈 · 2026-08-30（阶段 1 · 第 4 天 · 多线程收官）

## 学习内容
- 复盘：8-28 + 8-29 全盘复盘（共享内存 → 锁 → 协作 → 死锁 → 生产者消费者主线），焊点：sleep vs wait 对比、wait/notify 时间线推演、synchronized 方法 = synchronized(this)、jstack 正确用法（进程 pid ≠ 线程 id）、生产总量 = 消费总量程序才不卡死
- 整理修复：Lesson14 jstack 提示纠正、Lesson15 死代码清理（lock 字段 / Consumer.id / 未用 data）、8-29 两份文档错误结论修正（「jstack Windows 不可用」为错，实为全平台可用）；MyHashMap capacity() 恢复、LeetCode_88 无用 import 删除
- 新知 1：volatile（可见性）——VisibilityDemo 两轮实验：不加 volatile 时 main 置 false 后 worker 聋死（JIT 盯旧副本），加 volatile 立停；volatile 不保原子性（面试陷阱）
- 新知 2：死锁预防——统一加锁顺序破坏循环等待（Lesson14 预防版改造完成）
- 实操：jstack 全流程独立完成（jps -l 认 pid → jstack <pid> → 解读 Found one Java-level deadlock 的环）；CommandNotFoundException 排障（绝对路径 + & 调用）；Ctrl+C 停卡住程序

## 周进度
阶段 1 第 4 天。多线程线全部收官：创建/join/竞态/synchronized/wait-notify/死锁/生产者消费者/volatile/死锁预防 ✅。9 周核心计划三大块（手写 ArrayList、手写 HashMap、多线程）全部提前完成。

## 掌握程度
- ✅ 竞态条件与 synchronized：能解释丢失更新、锁的是「同一对象」这个资格
- ✅ volatile 可见性：VisibilityDemo 独立填空完成、两轮现象对答案
- ✅ 死锁预防：统一顺序改造成功，能说清循环等待被破坏
- ✅ jstack 全流程：jps 认 pid（排除 jps 自身与语言服务器干扰）、读 waiting/locked 环
- ⚠️ wait/notify 时序推演：标准时序已会判，变体时序（B 抢跑跳过 wait）刚认识
- ⚠️ 生产者消费者：跑通 + 结尾清库存解读会，环形指针推演熟练度待提

## 遗留问题
1. 总架构师「大三暑假」笔误待学生确认
2. 多线程线收官，下一课方向待定（候选：线程池 ExecutorService / 原子类 AtomicInteger；9 周期计划内容已提前完成大半，可请总架构师定方向或学生自选）
3. 今日全部改动待学生 git 提交

## 明日安排
git 提交收官今日内容。下一课：线程池或原子类二选一（学生自选或总架构师定），穿插 notifyAll 使用场景小点。
