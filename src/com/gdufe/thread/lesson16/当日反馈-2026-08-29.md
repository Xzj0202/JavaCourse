# 当日反馈 · 2026-08-29（阶段 1 · 第 3 天 · 多线程第二天）

> 重写说明：本文件于 2026-08-30 复盘后重写。原版两处硬错误已修正：①「jstack 在 Windows 不可用」——错，jstack 是 JDK 自带工具，Windows 完全可用，当时失败是因为给的命令本身错了（jstack 要的是进程 pid，不是线程 id）；②「明日从 8.24 补课」——幻觉，8-24 缺课内容已于 8-25 补齐。掌握程度栏按学生 8-30 自述（不太懂）如实降级。

## 学习内容
- wait/notify 机制：wait 放锁睡觉、notify 叫醒排队的线程，必须在 synchronized 内调用（WaitNotifyDemo）
- while + wait 防虚假唤醒：醒来必须重查条件
- 死锁构造：A 持 lock1 等 lock2，B 持 lock2 等 lock1 → 循环等待（DeadlockDemo）
- jstack 分析：jps -l 拿 pid → jstack <pid>（Windows 完全可用）
- 生产者消费者：环形缓冲区 + 满等/空等 + notify（ProducerConsumerDemo，2 产 2 消）

## 周进度
阶段 1 第 3 天。多线程四大块（创建/join/竞态/synchronized → wait/notify/死锁/生产者消费者）过完一轮。

## 掌握程度
- ⚠️ wait/notify：WaitNotifyDemo 跟写成功，时间线推演不熟（8-30 自述「不太懂」）
- ⚠️ 死锁：构造成功，jstack 当时未实操成功
- ⚠️ 生产者消费者：ProducerConsumerDemo 跑通过，环形指针与 notify 对象等细节未吃透
- ❌ jstack 用法（当时）：命令版本错误（线程 id ≠ 进程 pid），8-30 已纠正并修入 DeadlockDemo

## 遗留问题
1. 多线程内容 8-30 全盘复盘（已完成）
2. DeadlockDemo 提示文字 + ProducerConsumerDemo 死代码（lock 字段、Consumer.id、未用 data）（8-30 已修）
3. MyHashMap capacity() 已恢复、LeetCode_88 无用 import 已删（8-30 修复）
4. 总架构师「大三暑假」笔误待学生回复

## 明日安排
8-30：多线程新内容暂停一天，8-28 + 8-29 全盘复盘 + 三件套动手验收（WaitNotifyDemo 输出顺序、DeadlockDemo jstack 实操、ProducerConsumerDemo 不卡死）。
