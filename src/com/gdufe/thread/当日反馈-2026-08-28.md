# 当日反馈 · 2026-08-28（阶段 1 · 第 2 天 · 多线程第一天）

## 学习内容
- 线程概念：执行路径、CPU 时间片、并发假象、后端为什么需要
- 两种创建：Thread（extends）vs Runnable（implements）——单继承决定工程上永远用 Runnable
- start() vs run()：start 开新线程，run 只是普通调用（面试必考）
- join()：主线程等子线程；throws InterruptedException（受检异常初识）
- 竞态条件：i++ 三步（读-加-写）被线程切换插入 → 丢更新；四次实测 13249/20000/14002/14768——「有时对有时错」的随机性教训
- synchronized：锁对象、互斥、原子性、临界区；static synchronized 锁类对象
- NoClassDefFoundError：内部类字节码（$ 命名）丢失——报错对照表新增

## 周进度
阶段 1 第 2 天。多线程线：创建/join/竞态/synchronized ✅；剩余：wait/notify、死锁+jstack、生产者消费者。

## 掌握程度
- ✅ 三线程程序独立改写（Runnable + 3×start，输出交错）
- ✅ join 现象独立观察（主线程抢跑 vs 等待）
- ✅ RaceDemo 骨架独立写出，竞态四连跑坐实
- ✅ synchronized 一次修复成功（3×20000），锁粒度正确（只锁一行）
- ✅ 主动问出内部类 .class 生成规则（一个文件几个类几个 .class）
- ⚠️ .class 二进制文件别在编辑器打开/保存（第二次提醒）

## 遗留问题
1. .class 文件的处理习惯（第二次）
2. 昨日 MyHashMap 删注释时把 capacity() 也删了、main 仍调用——待学生二选一修复
3. 总架构师回复「大三暑假」疑为笔误（跨阶段旧待办）

## 明日安排
wait/notify（线程协作：等待与唤醒）→ 死锁构造 + jstack 分析 → 生产者消费者完整实现。
