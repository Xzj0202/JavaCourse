# 当日反馈 · 2026-08-17（阶段 0 · 第 4 天）

## 学习内容
- 翻译第 2 批：1. 两数之和（HashMap 一次遍历）、125. 验证回文串（双指针版，O(n)/O(1)）
- String 不可变：从 `s.charAt(i) = ...` 编译报错（需要:变量 找到:值）引出「石碑」类比
- `==` vs `equals`：基本类型比值 / 引用类型比地址；char 用 `==` 合法、String 必须 equals
- ClassNotFoundException 排查路径：看 out 里有没有 .class → 没有说明 javac 没成功
- 教学法调整：不逼学生故意写错代码（学生明确反馈）

## 周进度
阶段 0 第 4 天 / 14 天。20 题翻译完成 4/20（1、125、342、349）。

## 掌握程度
- ✅ 1. 两数之和：一次遍历 + 避开「字面量只能用于声明行」「每条路径要 return」两个坑
- ✅ 125. 验证回文串：独立写出双指针最优解，采纳 Character.isLetterOrDigit
- ✅ 编译报错自查：独立重编译贴红字，从报错定位到 String 不可变
- ✅ `==` vs `equals` 结论口头掌握（学生自述，未做实验验证）
- ⚠️ 测试习惯：main 里只写了一个用例，反例（race a car）未加——明日开始每题至少正反两例

## 遗留问题
1. 125 的反例测试未加（明日新题补齐此习惯）
2. `==` vs `equals` 口头掌握但未实战验证——后续字符串题自然检验
3. 总架构师回复「大三暑假」疑为笔误（跨天旧待办）

## 明日安排
翻译第 3 批 2 题；开场抽查：一句话说清 char 用 `==` 行、String 必须 equals 的原因；新题测试带正反两例。
